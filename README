1.缩进的空格数是可变的，但是同一个代码块的语句必须包含相同的缩进空格数。
2.基本数据类型:
	Numbers（数字）
	String（字符串）
	List（列表）  使用最频繁
	Tuple（元组）
	Sets（集合）
	Dictionaries（字典）
3.使用 del 语句删除一些对象引用。
4.Python 中的字符串不能改变。
5.元组（tuple）与列表类似，不同之处在于元组的元素不能修改且不能删除。但它可以包含可变的对象，比如 list 列表。
6.字典的关键字必须为不可变类型，且不能重复。